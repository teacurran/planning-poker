/**
 * REST API controllers and WebSocket endpoint handlers.
 * This package contains the primary adapters for external clients to interact with the application.
 *
 * <p>Subpackages:</p>
 * <ul>
 *   <li>{@code rest} - JAX-RS resource classes for RESTful endpoints</li>
 *   <li>{@code websocket} - WebSocket handlers for real-time communication</li>
 * </ul>
 */
package com.scrumpoker.api;
